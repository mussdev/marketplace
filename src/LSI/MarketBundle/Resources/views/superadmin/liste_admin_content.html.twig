<body>
    <div class="row well">
        <h2>Liste des administrateurs</h2>
        <table id="admin" class="table table-striped table-bordered">
            <thead>
            <tr class="success">
                <th>Nom d'utilisateur</th>
                <th>Adresse mail</th>
                <th>Date d'ajout</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for admin in listeAdmin %}
                <tr>
                    <td>{{ admin.username }}</td>
                    <td>{{ admin.email }}</td>
                    <td>{{ admin.dateInscription|date('d/m/Y H:m:s') }}</td>
                    <td>
                        {#<a role="button" class="btn btn-success" id="voir" href="{{ path('ls_imarket_voir_epci', {'id': epci.id}) }}">
                            <span class="glyphicon glyphicon-eye-open"></span> Consulter
                        </a>#}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">
                        <center><strong>Aucun admin crée !</strong></center>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</body>