{% extends 'LSIMarketBundle::monespace.html.twig' %}

{% block monespace %}
    {#Affichage des messages flashs #}
    {% for flash in app.session.flashbag.get('notice') %}
        <p class="alert alert-info">{{ flash }}</p>
    {% endfor %}
    {% for flash2 in app.session.flashbag.get('info') %}
        <p class="alert alert-info">{{ flash2 }}</p>
    {% endfor %}
    <center><strong><h2>CONDITIONS GENERALES D'UTILISATION</h2></strong></center>

    <div>
        <embed src="http://docs.google.com/gview?url={{ asset(cgu.webPath) }}&embedded=true" width="970" height="500" ></embed>
        {#<iframe src="{{ asset(cgu.webPath) }}" width="970" height="500" ></iframe>#}
        {#<img src='{{ asset(cgu.webPath) }}' width='800' height='500' type='application/pdf'/>#}
        {#<embed src='{{ asset(cgu.webPath) }}' width='800' height='500' type='application/pdf'/>#}
        {#<object data="{{ asset(cgu.webPath) }}" width="800" height="500" type="application/pdf"/>#}
    </div>
    <div class="row">
        <div class="form-group">
                <center><a role="button" href="{{ path('ls_imarket_modifier_cgu') }}" class="btn btn-warning">
                        <span class="glyphicon glyphicon-edit"></span> Modifier</a></center>
        </div>
    </div>
{% endblock monespace %}

